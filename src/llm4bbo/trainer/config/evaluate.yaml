defaults:
  - llm: qwen3-0.6b
  - _self_

hydra:
  run:
    dir: ${output_dir}

task: tf8
stage: base
num_designs: 500
output_dir: outputs/${wandb_init.name}

wandb_init:
  project: LLM4BBO
  dir: ${output_dir}
  name: "${hydra:runtime.choices.llm}/${task}/nd${num_designs}/${stage}/\
    ${now:%Y-%m-%d_%H-%M-%S}"

evaluate:
  num_trials: 8
  num_proposals: 128
  num_shots: 10
  thinking_budget: 12288
